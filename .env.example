#  Backend Configuration 
# Origin of the backend server proxied by Vite dev server (NOT exposed to browser)
# Change to your remote server or localhost
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000

# URL path prefix under which the backend lives - used by both proxy and browser
VITE_BACKEND_PATH=/backend

